/**
 *  Higher order function is a function that takes function as an argument or returns it or both.
 */

let numbers = [2, 4, 5, 6];
numbers.map((number) => number * 2);

setTimeout(() => {
  console.log("Hello");
}, 1000);
